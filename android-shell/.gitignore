build/
.gradle/
.gradle-home/
local.properties
snapshot/
app/src/main/assets/snapshot.tar.xz
# 快照指纹声明值（0.13.8-b ST-04 / F-ENV-01）：由构建链在拷贝 tar 后重写，与 tar 同属「本地产物」，
# 不入库——入库会造成「声明值是某一次单 ABI 构建的事实」，且与上面 tar 的忽略语义不一致。
# 出库后净检出/CI 下 tar 与 sha 均缺席，门禁打印 SKIP 计数；构建链以 --require 严格跑（缺席即失败）。
app/src/main/assets/snapshot.sha256
# 敏感数据（安全审计 2026-08-14）
*.credentials.yaml
token*
*.key
*.pem
*.p12
.kotlin/
# 插件构建产物（云端/本地 npm 构建生成，不入库；2026-08-31 收口）
plugins/*/lib/
plugins/*/node_modules/
plugins/*/cbin_*

# Python 编译缓存
__pycache__/

# 本地临时工作区（排查截图与中间产物）：0.14.0 两次误入提交后固化
.deploy-tmp/

# Generated first-build inputs and copied plugin/native assets.
app/src/main/jniLibs/
app/src/main/assets/plugins/
app/src/main/assets/licenses/voice-*
app/src/main/assets/patched/speech-services-composition.yml
app/src/main/assets/patched/startup-appearance-composition.yml
app/src/main/assets/patched/xiaomi-remote-composition.yml
