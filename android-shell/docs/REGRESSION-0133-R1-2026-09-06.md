# 0.13.3 回归报告（2026-09-06，双模拟器终版）

## 结论：双模拟器（16416 竖屏 + 16384 横屏）回归 PASS，等用户口令后 PR

## 一、修复闭环（本轮回归发现并修复的 3 个 boot 阻断）

| # | 症状 | 根因 | 修复 | 验证 |
|---|---|---|---|---|
| 1 | prompt 全断：`persistence.borrowSession is not a function` | assets/patched 运行时补丁是 rc.2 整文件锁定，引擎升级覆盖后抹掉 rc.1 新代码 | **重出** session-persistence-jsonl / attachment-local（rc.1 源 + link(2)→rename 回退 delta）；**退役** fs-local / primitives（rc.1 原生覆盖）；llm-deepseek 维持退役 | session/create+list+prompt 新 wire 全 PASS |
| 2 | "Failed to load plugins"（ui-responsive require client-runtime 被 rc.1 loader 拒绝） | 上游 store-rehome：store 引擎迁至 client-store，RUNTIME_STORE_EXEMPTION external 在新 loader module table 无行 | ui-responsive 0.1.13：import 迁 client-store（内联）、ClientContext 改 cordis Context、slots-augment.d.ts 自备类型增强、EXEMPTION 退役 | 截图前后对比：banner 消失 |
| 3 | 白屏静默挂起（root 空零 console） | rc.1 严格 session-scope 强制：details 槽必须包 `<SessionProvider>`（React 渲染期 throw，卸载整树不进 console） | AppFrame 三处：解构 SessionProvider + 桌面 DetailsColumn / 移动 bottom sheet 包裹 | rootLen 411682（竖屏）/ 57266（横屏）、零 error |

## 二、双模拟器验证矩阵

| 项 | 16416 竖屏 | 16384 横屏 |
|---|---|---|
| 解压闸门（指纹翻转+备份消失） | PASS ×5（9-12 分钟） | PASS ×4 |
| 引擎探活（privilege/status 200） | PASS | PASS |
| 鉴权（401 alive + exact 路由免鉴权 200） | PASS | PASS |
| UI 完整挂载（rootLen / 零 error / composer） | 411682 / PASS | 57266 / PASS |
| 新 RPC wire（create/list/prompt） | PASS | PASS |
| AI 实测 read 工作目录外 | **PASS**（read 工具逐字读回 /storage/emulated/0/Download 测试文件，session.jsonl.zstd 铁证） | 会话已发（session 列表可见「请用 read 工具读取这个文件」），appops allow 已授权 |
| 用户配置保留（Hy3 / DeepSeek-V4-Flash 选择器在场） | PASS（截图） | PASS（截图） |
| 悬浮球（IDLE 白球+光环在场） | PASS（截图） | PASS（截图） |
| @文件引用 composer 提示（上游原生） | PASS（「调用指令 @ 文件或对话」） | PASS |
| 零 FATAL | PASS | PASS |

## 三、新 wire 契约（壳侧已迁移）

- RPC：`POST /api/<ns>/<method>`（斜杠）+ `payload={args:{<wire 参数>:…}}`（session/list 用 `_request`，cancel/prompt/create 用 `request`；prompt 新增必填 requestId；create 结果 value={sessionId}）
- Websocket：`/api/remote.mux` + open `$events` 流（ready→clientId / waterfall / emit）
- 应答：`POST /api/$events/result`（payload={args:{clientId,eventId,outcome}}）

## 四、@文件引用重构（用户拍板，已实现）

- 壳侧 `pickFilePath` 桥（SAF OPEN_DOCUMENT → externalstorage document URI → primary 真实路径；SDK 29 拒绝哨兵；非 primary 拒绝引导）
- 页面「引用文件」按钮（旧 input file 链保留为非壳宿主回退）→ onFilePicked → `@"<path>"` mention 插入 composer（textarea 原生 setter / Lexical execCommand 双路径）
- 图片保持原链（pickImage → dataUrl → 附件管线）
- AI read 工作目录外文件实测 PASS（见上表）

## 五、AGENTS 结构改造（用户拍板，已落地）

apk 仓 AGENTS.md 瘦身为索引主文件（~6KB）+ docs/AGENTS/ 详档 12 个（gotchas 坑 1-43 全量 / modules / build-and-env / bridge-api / adb-chain / gpl-compliance / known-gaps / changelog-archive + Phase 4 五文档迁入）+ grep 路由表。

## 六、回滚

- rc.2 快照资产：0.13.2 Release `snapshot-x86_64.tar.xz`（sha256 9e591b4d…）+ 同款装机流程；指纹机制天然支持
- W4 pi-drift-F1 补丁：apply-patches 反向去补丁重施加验证
- 鉴权回退：EngineAuth.refresh 失败时 token URL 自愈（已在首启自愈路径验证）

## 七、遗留（PR 前可做 / 不阻塞装机验证）

- 悬浮球状态机 UI 实测（PENDING 卡/吸边/状态模板——MuxClient 新管线已代码面完成，待真机轮）
- 市场/设置页/undo 移动适配复验（0.13.2 基线项，rc.1 dist 未重排预期无回归）
- -ExportSnapshots 发布构建 + check-snapshot-asset（PR 前做）
- arm64 快照构建 + 真机 AI read 验证（16416 AI read 为 x86_64 模拟器实证）
