现在继续第二阶段：请你亲自编写一个原生安卓计数器App，然后在这台手机内编译、安装、运行、检查。项目就在当前 /storage/emulated/0/work/phone-app-lab，不要操作其他项目，不要安装/覆盖DeepCode本身，不要读取账号、ADB私钥等秘密。
要求：包名只能是 com.deepcode.selfhost.counter，MainActivity.java用Android标准View编写，不依赖网络库、AndroidX或Gradle。写src/com/deepcode/selfhost/counter/MainActivity.java和AndroidManifest.xml；minSdk26,targetSdk34,versionCode1,versionName1.0,debuggable=true,导出的启动MainActivity；无权限申请。界面简洁美观，深色背景，中文标题“手机自己造的 App”，说明“代码、编译和安装，都在这台手机完成”，大数字计数，明显的“+1”和“归零”按钮，版本文字“本机开发 · 第1版”。正常启动计数0。
给debug实验提供 self_test 布尔intent extra：为true时在UI准备好后通过按钮performClick真正触发三次同一个加一监听器，校验计数==3，使用Log.i("PhoneAppLab","SELF_TEST_PASS revision=1 count=3")或失败标志；默认不自动点击。expected_revision extra应校验版本。保留公开构建源码。
工具链已装好。因为构建脚本刚补上了安装校验收据，请先把 /data/user/0/com.dsharnessmobile.shell/files/home/.dsh/android-app-lab/bootstrap/build-app.sh 复制到 /data/user/0/com.dsharnessmobile.shell/files/home/.dsh/debian/current/root/android-app-lab/build-app.sh（都在应用权限目录），然后使用既有media.py --workspace 当前项目 --timeout 300 -- bash /root/android-app-lab/build-app.sh，输出记录build-v1.log。所有编译必须在手机Debian完成，不要用远程编译，不要把源码发给外部服务构建。如果遇到兼容错误，定位修复并重试。
编译成功后用Termux python调用 /data/user/0/com.dsharnessmobile.shell/files/home/.dsh/android-app-lab/phone-adb.py，先status，再install，再launch --revision 1 --self-test。此脚本复用DeepCode自己已经授权的本机ADB身份，明确允许安装这个独立测试App。等待UI和自测完成后capture --revision 1以及logs，将日志记录adb-v1.log。最后执行return回到DeepCode，避免长时间后台冻结。
如果系统拒绝安装或注入操作，明确记录真实错误，不得假称安装/交互通过。不要绕过系统安装确认。检查截图如有图像预览能力就打开图片，最后报告源码文件、APK路径、构建耗时、安装结果和自测日志。注意程序内performClick测试不等于真实手指触摸测试。
